# --- Web framework ---
Flask==2.3.2
flask-cors==3.0.10
gunicorn==21.2.0

# --- HTTP requests & environment ---
requests==2.31.0
python-dotenv==1.0.0

# --- Computer vision (headless for server) ---
opencv-python-headless==4.8.1.78

# --- Audio playback (server-safe / lightweight) ---
playsound==1.3.0
simpleaudio==1.0.4
pydub==0.25.1

# --- Optional: Other utilities you might need ---
numpy==1.27.0
